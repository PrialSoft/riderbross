-- OBSOLETO — contenido fusionado en supabase-schema-completo.sql (raíz del repo).
-- No ejecutar este archivo en bases nuevas; evita divergencia con el esquema unificado.

SELECT 1; -- no-op para editores que esperan SQL válido
